{
    "name": "sample-obsidian-theme",
    "version": "0.2.0",
    "scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"build:css": "sass src/main.scss:./theme.css --no-source-map",
		"watch:css": "sass src/main.scss:./theme.css --watch"
    },
    "devDependencies": {
        "sass": "^1.89.2"
    }
}
