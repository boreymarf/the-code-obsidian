{
    "name": "sample-obsidian-theme",
    "version": "1.0.0",
    "scripts": {
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"build:css": "sass src/main.scss:./theme.css --no-source-map --style=compressed",
		"watch:css": "sass src/main.scss:./theme.css --watch --style=compressed"
    },
    "devDependencies": {
        "sass": "^1.89.2"
    }
}
